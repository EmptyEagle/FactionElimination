using System;
using System.Collections.Generic;
using ItemChanger;
using ItemChanger.Placements;
using ItemChanger.StartDefs;


namespace FactionElimination
{
    class SlyShop
    {
        //public StartDef StartDef => TransitionBasedStartDef.FromGate(SceneNames.Room_shop, "left1");

        //ShopPlacement sly = Finder.GetLocation(LocationNames.Sly).Wrap() as ShopPlacement;

        //AbstractItem dive = Finder.GetItem(ItemNames.Desolate_Dive);

        //sly.AddItemWithCost(dive, 100);
    }
}